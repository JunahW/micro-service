spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JunahW/service-config/
          searchPaths: config
          username:
          password:
          default-label: master
        bootstrap: true

eureka.client.serviceUrl.defaultZone: http://localhost:9001/eureka
